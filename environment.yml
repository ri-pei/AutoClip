name: autoclip
channels:
  - conda-forge
dependencies:
  # Portable dependency specification, not a machine-specific lockfile.
  - python=3.12
  - ffmpeg>=7
  - numpy>=1.26
  - scipy>=1.11
  - pandas>=2
  - opencv>=4
  - pillow
  - imagehash
  - scikit-learn
  - pip
